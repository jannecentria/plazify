{
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "npx live-server frontend --port=3000",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "live-server": "^1.2.0"
  }
}
